import AntdButton from '@/components/antd/Button';

const CancelButton = () => {
  return (
    <AntdButton type="default" danger shape="round">
      Annuleren
    </AntdButton>
  );
};

export default CancelButton;
