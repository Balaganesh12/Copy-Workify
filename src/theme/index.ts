import ComponentToken from './ComponentToken';
import GlobalToken from './GlobalToken';

const antdThemeTokens = {
  token: GlobalToken,
  components: ComponentToken,
};

export default antdThemeTokens;
