import ButtonToken from './Button';
import DividerToken from './Divider';
import FormToken from './Form';
import InputToken from './Input';
import LayoutToken from './Layout';
import ModalToken from './Modal';
import SelectToken from './Select';
import TableToken from './Table';

const ComponentToken = {
  Divider: DividerToken,
  Input: InputToken,
  Layout: LayoutToken,
  Form: FormToken,
  Select: SelectToken,
  Table: TableToken,
  Button: ButtonToken,
  Modal: ModalToken,
};

export default ComponentToken;
